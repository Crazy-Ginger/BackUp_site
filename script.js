function runSubmit()
{
    
}

function clearText(id)
{
    document.getElementById(id).value = "";
}

//implement an algorithm that hashes the users files and then sends that file to the server to check against the server's own hashes
